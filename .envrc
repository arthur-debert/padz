#!/bin/bash

# Universal project environment variables
export PROJECT_ROOT="$PWD"
export PKG_NAME="padz"
export GH_USER="arthur-debert"
export GH_URL="https://github.com/arthur-debert/padz"

# Derived URLs
export GH_REPO="arthur-debert/padz"
export GH_CLONE_URL="git@github.com:arthur-debert/padz.git"

# Add project's bin directory to PATH
export PATH="$PROJECT_ROOT/bin:$PATH"

# Go-specific environment
export GOPATH="${GOPATH:-$HOME/go}"
export PATH="$PATH:$GOPATH/bin"

# Development tools
if command -v direnv >/dev/null 2>&1; then
    echo "✅ direnv loaded for padz"
else
    echo "💡 Install direnv for automatic environment loading: brew install direnv"
fi
